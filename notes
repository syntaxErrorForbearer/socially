# sym linked "ln -s /usr/bin/nodejs /usr/bin/node" -- added symbolic link for typing init command

# echo fs.inotify.max_user_watches=524288 | sudo tee -a /etc/sysctl.conf && sudo sysctl -p
# It looks like a simple tweak to your system's configuration will make many
#   tools (including this Meteor command) more efficient. To learn more,
# command from
#://github.com/meteor/docs/blob/version-NEXT/long-form/file-change-watcher-efficiency.md Adding this to fix typings init command in the angular - meteor tutorial
# ***I'm not 100% sure this was needed***removed and didnt see a difference or warning***

